<h1>Filter</h1>


<% if @students.length == 0 %>
<!--empty filter error--->
  <div id="empty" >
    <h2>We are sorry, 
    <br><br>
    there are no students
    <br><br>
    in this filter</h2>  
  </div>
<% end %>

<br>
<table class="filter">
  <% @students.each do |student| %>
      <tr>
        
          <div class="filter">
            <%= link_to student.name %> <%=student.surname %> <%=student.school %> <%=student.comment %>
          </div>
         
          <div class="links">
            <% if user_signed_in? %>
              <%= link_to 'Edit', edit_student_path(student) %>
              <%= link_to 'Destroy', student, method: :delete, data: { confirm: 'Are you sure?' } %>
            <%else%>
            
            
            <%end%>  
        </div>
        </td>
      </tr>
    <% end %>
</table>
